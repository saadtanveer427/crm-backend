export enum UserRole {
  ADMIN = 'admin',
  MEMBER = 'member',
}

export enum ActivityAction {
  CREATED = 'created',
  UPDATED = 'updated',
  DELETED = 'deleted',
  RESTORED = 'restored',
  ASSIGNED = 'assigned',
  NOTE_ADDED = 'note_added',
}
